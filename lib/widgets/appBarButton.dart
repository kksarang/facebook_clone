import 'package:flutter/cupertino.dart';

class AppBarTheme extends StatelessWidget {
  const AppBarTheme({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
